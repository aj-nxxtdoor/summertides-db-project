-- Create and initialize the SummerTides Festival database.
-- This script is intentionally lightweight for SQLite so it can be executed directly.
PRAGMA foreign_keys = ON;

SELECT 'SummerTides database ready for schema setup.' AS status;